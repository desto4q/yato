import 'package:flutter/material.dart';

class TabDummy extends StatelessWidget {
  const TabDummy({super.key});

  @override
  Widget build(BuildContext context) {
     return Center(
      child: Text("dummy"),
    );
  }
}